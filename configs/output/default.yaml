# Output / job identification settings.

jobtype: experiment       # Used to name output directories:
                          #   outputs/training/{jobtype}/{timestamp}/
                          #   outputs/hpo/{jobtype}/{timestamp}/
                          #   outputs/prediction/{jobtype}/{timestamp}/
