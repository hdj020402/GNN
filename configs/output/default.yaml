# Output / job identification settings.

jobtype: experiment       # Used to name output directories:
                          #   Training_Recording/{jobtype}/{timestamp}/
                          #   HPTuning_Recording/{jobtype}/{timestamp}/
                          #   Prediction_Recording/{jobtype}/{timestamp}/
